<%= form_for(@song) do |f| %>
  <% if @song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% @song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.text_field :name, class: 'form-control', placeholder: 'Input Song Name' %>
  </div>
  <div class="form-group">
    <%= f.label :rating %>
    <%= f.select :rating, [1, 2, 3, 4, 5], {:include_blank => 'Rate this song'}, class: 'form-control'%>
  </div>
  <div class="form-group">
    <%= f.label :artist_id %>
    <%= f.collection_select :artist_id, @artists, :id, :name, {:include_blank => 'Select an Artist'}, class: 'form-control' %>
  </div>
  <div class="form-group">
<%= f.text_field :video, class: 'form-control', placeholder: 'YouTube Video Link' %>
  </div>
  <div class="actions">
    <%= f.submit "Fuck Art", class: 'btn btn-default' %>
  </div>
<% end %>
